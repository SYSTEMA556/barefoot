

<div class="d-flex flex-column flex-sm-row">

  <div class="container my-5" style="max-width: 1100px;">
    <form method="get" action="<%= novels_path %>">
<input type="radio" name="sort" id="sort_comments" value="comments"
<%= 'checked' if params[:sort] == 'comments' %>>
<label for="sort_comments">コメント数順</label>

<input type="radio" name="sort" id="sort_views" value="views"
<%= 'checked' if params[:sort] == 'views' %>>
<label for="sort_views">閲覧数順</label>

<input type="radio" name="sort" id="sort_bookmarks" value="bookmarks"
<%= 'checked' if params[:sort] == 'bookmarks' %>>
<label for="sort_bookmarks">ブックマーク数順</label>

<input type="radio" name="sort" id="sort_updated" value="updated_at"
<%= 'checked' if params[:sort] == 'updated_at' %>>
<label for="sort_updated">更新日時順</label>

<button type="submit">ソートする</button>
</form>
   
          <!-- 上部ページネーション -->
  <!-- 上部ページネーション（will_paginate 用） -->
<nav aria-label="ページネーション" class="mt-4">
  <div class="d-flex justify-content-center">
    <%= will_paginate @novels %>
  </div>
</nav>

    <!-- 📚 作品カード一覧 -->
    <section>
      
      <div class="row row-cols-1 row-cols-lg-2 g-4">
        <% @novels.each do |novel| %>
          <div class="col">
            <%= link_to novel_path(novel), class: "text-decoration-none text-dark" do %>
              <div class="card h-100 shadow-sm border-0 bg-body">
                <div class="card-body d-flex flex-column p-3">
                  <!-- タイトル -->
                  <h5 class="card-title mb-2 fs-6">
                    <i class="bi bi-music-note me-1"></i>
                    <%= truncate(novel.title, length: 28) %>
                  </h5>

                  <!-- 作者 -->
                  <div class="card-subtitle mb-2 text-muted small">
                    <% if novel.user %>
                      <span class="badge bg-primary me-1">会員</span>
                      <%= novel.user.user_name %>
                    <% elsif novel.author_name.present? %>
                      <span class="badge bg-secondary me-1">ゲスト</span>
                      <%= h(novel.author_name) %>
                    <% else %>
                      <em class="text-muted">作者不明</em>
                    <% end %>
                  </div>

                  <!-- 下書きバッジ -->
                  <% if novel.respond_to?(:status) && novel.draft? %>
                    <span class="badge bg-secondary mb-2">下書き</span>
                  <% end %>

                  <!-- 本文抜粋 -->
                  <p class="card-text small text-body flex-grow-1 lh-sm">
                    <%= truncate(novel.body, length: 100) %>
                  </p>

                  <!-- メタ情報 -->
                  <div class="mt-3 d-flex justify-content-between align-items-end small text-muted">
                    <div>
                      <%= novel.published? ?
                          "投稿日：" + l(novel.created_at, format: :custom_ja) :
                          "更新日：" + l(novel.updated_at, format: :short) %>
                    </div>
                    <div>
                      💬 <%= novel.respond_to?(:comments_count) ? novel.comments_count : (novel.comments.size rescue 0) %>
                    </div>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>

   <!-- 下部ページネーション（will_paginate 用） -->
<nav aria-label="ページネーション" class="mt-4">
  <div class="d-flex justify-content-center">
    <%= will_paginate @novels %>
  </div>
</nav>
    </section>

  </div>
</div>
